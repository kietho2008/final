apiVersion: v1
kind: Service
metadata:
  name: carrental-mvc
spec:
  selector:
    app: carrental-mvc
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30045
  type: NodePort
