apiVersion: v1
kind: Service
metadata:
  name: vehicleinventory-api
spec:
  selector:
    app: vehicleinventory-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
